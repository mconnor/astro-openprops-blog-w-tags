---
// import AstroNavigateSelect from '@/components/AstroNavigateSelect.astro';
// import { fade } from 'astro:transitions';
// interface ImageMetadata {
//   src: string;
//   width: number;
//   height: number;
//   format: string;
// }
import { Image } from 'astro:assets';
// import Andy from '@/assets/andyBushes.jpg';
import AndyMud from '@/assets/andy_mud_color.webp';
import Layout from '@/layouts/BaseLayout.astro';
import { Stack, Frame } from '@cc/index.ts';
import { Box } from '@/lit-layout-components/index.ts';



---

<Layout title="home">
 
  <Stack space="0">
    
    <Box borderWidth="0" padding="var(--s0)" >
      <h1>Mike Connor, developer</h1>
      <p>Welcome to my long overdue public shingle.</p>
 
    
    </Box>

    <Box invert borderWidth="1px" padding="var(--s0)">
      <p>This my dog Andy. He's purebred mutt from the shelter in New Orleans.</p>
      <Frame ratio="16:9">
        <Image
          src={AndyMud}
          widths={[240, 540, 720, AndyMud.width]}
          sizes={`(max-width: 360px) 240px, (max-width: 720px) 540px, (max-width: 1600px) 720px, ${AndyMud.width}px`}
          alt="A description of my image."
        />
      
      </Frame>
     </Box
    >

       <Box borderWidth="0" padding="var(--s0)"><p>We currently reside in Old Westport, KCMO</p></Box>

  

  </Stack>
</Layout>

<style>
  h1{
    font-size: var(--font-size-fluid-2);

    /* @media (--motionOK) {
    animation: var(--animation-fade-in); */
  }
  p{
    font-size: var(--font-size-fluid-1);
    font-weight: var(--font-weight-5);

  }
  caption {
 
    font-size: var(--font-size-3);
  }
  /* h3 {
 
 font-size: var(--font-size-2);
} */
  .copy {
   
    font-size: var(--font-size-2);
  }

  /* .circle-mask {
    width: 140px;
    height: 140px;
    border-radius: 50%;
    overflow: hidden;
  } 


img {

    object-fit: none;
  }*/
</style>
