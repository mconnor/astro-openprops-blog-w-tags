---
import { BoxClass } from './box-l';
import type { RmUnitType } from '../Types';

interface Props {
  padding?: RmUnitType;
  borderWidth?: RmUnitType;
  invert?: boolean;
}
const props = Astro.props;
---

<BoxClass {...props} client:visible>
  <slot />
</BoxClass>
