---
import { BoxClass } from './box-l';
import type { RmUnitType, BorderStyle } from '#unit-types';

interface Props {
  padding?: RmUnitType;
  borderWidth?: RmUnitType;
  invert?: boolean;
  highlight?: boolean;
  borderStyle?: BorderStyle;
}
const props = Astro.props;
---

<BoxClass {...props} client:visible>
  <slot />
</BoxClass>
