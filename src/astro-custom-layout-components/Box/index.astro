---
import './Box.css';
import type { RmUnitType } from '../Types';

interface Props {
  padding?: RmUnitType; //"var(--s1)"
  borderWidth?: RmUnitType; //"var(--border-thin)"

  invert?: boolean; // false
}
const {
  padding = 'var(--s1)',
  borderWidth = 'var(--border-size-1)',
  invert = false,
} = Astro.props;
---

<box-l padding={padding} borderWidth={borderWidth} invert={invert}>
  <slot />
</box-l>
<script src="./Box.js"></script>
