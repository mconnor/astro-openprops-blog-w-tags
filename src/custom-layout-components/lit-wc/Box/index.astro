---
import type { BorderStyle, RmUnitType } from '#UnitTypes.ts';

interface Props {
  padding?: RmUnitType;
  borderWidth?: RmUnitType;
  highlight?: boolean;
  borderStyle?: BorderStyle;
  invert?: boolean;
  children?: unknown;
  active?: boolean;
}
const { borderWidth, borderStyle, ...rest } = Astro.props;
---

<script>
  import './Box.ts';
</script>

<box-l border-width={borderWidth} border-style={borderStyle} {...rest}>
  <slot />
</box-l>
