---
import { Icon } from 'astro-icon/components';

interface Props {
  size?: number;
}

const { size = 50 } = Astro.props;
---

<!-- Loads the SVG in `/src/icons/filename.svg` -->
<Icon name="logos/twitter" size={size} />
