---
import type { HTMLTag, Polymorphic } from 'astro/types'

type Props<Tag extends HTMLTag> = Polymorphic<{
    as: Tag
    bgColor?: string
}>

const { as: Tag, class: className, bgColor = 'red', ...rest } = Astro.props
---

<Tag class={className} {...rest}><slot /></Tag>
